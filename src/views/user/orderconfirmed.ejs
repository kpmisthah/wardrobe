<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation | Wardrobe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Modern Success Page Styles */
        body {
            background-color: #f8f9fa;
        }

        .success-card {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            padding: 50px 30px;
            text-align: center;
            max-width: 600px;
            margin: 60px auto;
            border: 1px solid #eee;
        }

        /* Animation for checkmark */
        .checkmark-circle {
            width: 80px;
            height: 80px;
            position: relative;
            display: inline-block;
            vertical-align: top;
            margin-bottom: 20px;
        }

        .checkmark-circle .background {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: #28a745;
            position: absolute;
        }

        .checkmark-circle .checkmark {
            border-radius: 5px;
        }

        .checkmark-circle .checkmark.draw:after {
            animation-delay: 100ms;
            animation-duration: 3s;
            animation-timing-function: ease;
            animation-name: checkmark;
            transform: scaleX(-1) rotate(135deg);
        }

        .checkmark-circle .checkmark:after {
            opacity: 1;
            height: 40px;
            width: 20px;
            transform-origin: left top;
            border-right: 4px solid white;
            border-top: 4px solid white;
            content: '';
            left: 20px;
            top: 40px;
            position: absolute;
        }

        h1 {
            font-family: 'Playfair Display', serif;
            color: #333;
            margin-bottom: 10px;
            font-weight: 700;
        }

        p.lead {
            font-size: 16px;
            color: #666;
            margin-bottom: 30px;
        }

        .order-info {
            background: #f9f9f9;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 30px;
            font-size: 14px;
            color: #555;
        }

        .btn-custom {
            padding: 12px 30px;
            border-radius: 50px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 12px;
            transition: all 0.3s;
            border: none;
            margin: 5px;
        }

        .btn-black {
            background: #333;
            color: #fff;
        }

        .btn-black:hover {
            background: #555;
            color: #fff;
            transform: translateY(-2px);
        }

        .btn-outline {
            border: 2px solid #ddd;
            background: transparent;
            color: #333;
        }

        .btn-outline:hover {
            border-color: #333;
            background: #333;
            color: #fff;
        }

        .conf-text {
            font-size: 13px;
            color: #999;
            margin-top: 20px;
        }
    </style>
</head>

<body>
    <%- include('../../views/partials/userPartials/header') %>

        <div class="container pb-5">
            <div class="success-card">
                <div class="checkmark-circle">
                    <div class="background"></div>
                    <div class="checkmark draw"></div>
                </div>

                <h1>Thank You!</h1>
                <p class="lead">Your order has been successfully placed.</p>

                <div class="order-info">
                    <div class="row">
                        <div class="col-12">
                            <p class="mb-1"><strong>Order ID:</strong> <span
                                    style="font-family: monospace; color: #717fe0;">
                                    <%= orderId %>
                                </span></p>
                            <p class="mb-0">You will receive an email confirmation shortly at your registered email
                                address.</p>
                        </div>
                    </div>
                </div>

                <div class="action-buttons">
                    <a href="/shop" class="btn btn-custom btn-black">
                        Continue Shopping
                    </a>
                    <a href="/orders" class="btn btn-custom btn-outline">
                        View My Orders
                    </a>

                    <input type="hidden" name="orderId" id="orderIdInput" value="<%=orderId%>">
                    <button id="downloadInvoice" class="btn btn-custom btn-outline"
                        style="border-color: #28a745; color: #28a745;">
                        <i class="zmdi zmdi-download"></i> Invoice
                    </button>
                </div>

                <p class="conf-text">Need help? <a href="/contact" style="color:#717fe0;">Contact Support</a></p>
            </div>
        </div>

        <%- include('../../views/partials/userPartials/footer') %>

            <!-- Scripts -->
            <script src="/javascript/orderConfirmed.js"></script>
            <script>
                // Inline script to handle simple invoice download trigger if external JS fails
                document.getElementById('downloadInvoice').addEventListener('click', function () {
                    const orderId = document.getElementById('orderIdInput').value;
                    // Assuming a route exists like /order/download/pdf/:id
                    window.location.href = `/order/download/pdf/${orderId}`;
                });
            </script>
</body>

</html>